{{follow-project project=model class='h1'}}

<h1 class="dib" data-model-id={{model.id}}>{{model.name}}</h1>

<ul class="nav nav-tabs">
  {{#active-link}}
    {{#link-to 'project.edit.edit' model}}Edit{{/link-to}}
  {{/active-link}}

  {{#active-link}}
    {{#link-to 'task' model}}Tasks{{/link-to}}
  {{/active-link}}
</ul>

<br>

{{liquid-outlet}}
