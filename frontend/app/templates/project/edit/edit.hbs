{{#if model.isNew}}
  <h1>Create project</h1>

  <br>
{{/if}}

<form class="form-horizontal" {{action 'submit' on='submit'}}>
  <div class="form-group">
    <label for="name" class="col-sm-2 control-label">Name</label>
    <div class="col-sm-10">
      {{input id='name' placeholder='Project name' value=model.name class='form-control' autocomplete='false'}}
    </div>
  </div>
  <div class="form-group">
    <label for="customer" class="col-sm-2 control-label">Customer</label>
    <div class="col-sm-10">
      {{ember-selectize
        id              = 'customer'
        content         = customers
        selection       = model.customer
        optionLabelPath = 'content.name'
        optionValuePath = 'content.id'
        }}
    </div>
  </div>
  <div class="form-group">
    <label for="tracker" class="col-sm-2 control-label">Bugtracker</label>
    <div class="col-sm-10">
      {{ember-selectize
        id        = 'tracker'
        content   = trackers
        value     = model.tracker.type
        }}
    </div>
  </div>
  {{#if model.tracker.type}}
    {{partial trackerPartial}}
  {{/if}}
  <div class="form-group">
    <label for="from" class="col-sm-2 control-label">From</label>
    <div class="col-sm-10">
      {{date-picker id='from' date=model.from}}
    </div>
  </div>
  <div class="form-group">
    <label for="to" class="col-sm-2 control-label">To</label>
    <div class="col-sm-10">
      {{date-picker id='to' date=model.to}}
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <div class="checkbox">
        <label>
          {{input type='checkbox' checked=model.done}}
          Done
        </label>
      </div>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-primary">
        {{#if model.isNew}}
          Create project
        {{else}}
          Update project
        {{/if}}
      </button>

      {{#unless model.isNew}}
        <button type="button" {{action 'delete' on='doubleClick'}} class="btn btn-danger pull-right tip" title="Doubleclick to delete">
          Delete project
        </button>
      {{/unless}}
    </div>
  </div>
</form>
