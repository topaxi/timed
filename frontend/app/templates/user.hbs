<div class="row">
  <div class="col-md-2">
    <ul class="nav nav-pills nav-stacked">
      {{#active-link}}
        {{#link-to 'user.index'}}Users{{/link-to}}
      {{/active-link}}

      {{#active-link}}
        {{#link-to 'user.new'}}Create user{{/link-to}}
      {{/active-link}}
    </ul>
  </div>

  <div class="col-md-10">{{liquid-outlet}}</div>
</div>
