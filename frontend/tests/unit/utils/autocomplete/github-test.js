import autocompleteGithub from 'timed/utils/autocomplete/github';

module('autocompleteGithub');

// Replace this with your real tests.
test('it works', function() {
  var result = autocompleteGithub();
  ok(result);
});
